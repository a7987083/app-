# Automatic online release switch used by successful project CI workflows.
# Keep this file in release artifacts/policy history so future maintainers know
# that a green CI for a new VERSION must dispatch the canonical release flow.
AUTO_RELEASE=1
RELEASE_WORKFLOW=phase13_github_release.yml
RELEASE_TAG_PREFIX=source-v
