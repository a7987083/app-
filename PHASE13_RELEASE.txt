ZONOE 软件源 Release 2026091605
版本：2026091605
基线：2026091604

更新内容：
1. Phase 15.1 新增只读生产数据完整性审计：重复/空卡密、授权 UDID 异常、换绑额度异常、唯一索引状态。
2. 新增授权中心“数据完整性”后台页面，展示重复卡密样本和后续 UNIQUE INDEX 迁移前提。
3. 本版不创建 UNIQUE INDEX，不修改或删除生产卡密数据；先在线升级读取真实生产审计结果，再决定下一步迁移。
4. 固定 /unbind 当前稳定语义为 active_entitlements_only：仅迁移 jh=1 且 endtime>当前时间的有效授权行，过期历史卡保持原 UDID。
5. 新增 180 天长期保留候选统计：过期黑名单、旧授权事件、旧换绑日志只审计不删除。
6. Phase 15 PHP 7.0 CI 与正式 Release 在线更新包内容检查已加入发布流水线。

兼容性：
- 兼容宝塔 PHP 7.0。
- 不修改数据库结构、不新增唯一索引、不删除历史数据。
- appstore/appstore_v2、Nuosike 加密、UDID、卡密时长、黑名单、transfer_count 语义保持不变。
- 保留 GitHub 在线更新、SHA256、备份、文件校验与回滚机制。
