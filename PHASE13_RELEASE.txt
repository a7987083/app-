ZONOE 软件源 Release 2026091706
版本：2026091706
基线：2026091705

更新内容：
1. 删除卡密时同步删除 fa_kami_app 中该 kami_id 的指定 App 授权映射。
2. 删除 App 时同步删除 fa_kami_app 中该 app_id 的指定 App 授权映射。
3. 卡密仅到期不清映射；App 仅隐藏/停用/编辑也不清映射，只有真正删除时触发。
4. 映射清理使用 ThinkPHP beforeDelete 模型事件，继续处于 FastAdmin 删除事务内，单删/批量删均生效。
5. 清空换绑记录成功后跳回授权总览 #transfer-preview，并通过唯一 _refresh 参数强制重新请求。
6. 清空授权事件成功后跳回授权总览 #event-preview，并通过唯一 _refresh 参数强制重新请求。
7. 授权总览增加 no-store/no-cache 响应头，避免浏览器后退恢复清空前的旧预览快照。
8. 在线更新清单新增 application/admin/model/Kami.php 与 application/common/model/Category.php。
9. 不自动删除历史孤儿映射；现有异常继续通过数据完整性页面审计。
10. Phase17.5 PHP 7.0 契约测试通过。

数据库：
- 不新增数据库字段，不改变 fa_kami_app 表结构。
- 到期数据继续保留映射；真正删除卡密/App 时同步清映射。

在线更新：
- GitHub Release 标签：source-v2026091706。
- 资产：zonoe-online-update.zip + zonoe-online-update.zip.sha256。
- 从 2026091705 可通过后台 GitHub 在线更新直接升级到 2026091706。
- 保留 HTTPS、SHA256、数据库/程序备份、文件覆盖校验、失败回滚和真实 Release E2E。

兼容性：
- 兼容宝塔 PHP 7.0、MySQL 5.7。
- 不改变三种卡密用途、一次性消费规则、授权叠加语义、apiface 签名和续费入口协议。
