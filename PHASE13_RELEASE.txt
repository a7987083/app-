ZONOE 软件源 Release 2026091601
版本：2026091601
基线：2026091203

更新内容：
1. appstore 与 appstore_v2 软件源加密默认改为本地兼容实现。
2. 保留 Nuosike 故障回退，支持整体回退和单独关闭 v2 本地加密。
3. 保持现有软件源协议字段、请求头识别方式、UDID、卡密、授权时长、黑名单与换绑次数语义不变。
4. 新增 SourceEncryptionProvider 与 SourceEncryptionPolicy，并接入 App.php 软件源主链路。
5. 新增 PHP 7.0 / DES-CBC 兼容测试和差分探针工具。
6. 在线更新包包含本次加密与认证链路所需文件，并继续使用现有备份、回滚和 SHA256 校验。

兼容性：
- 兼容宝塔 PHP 7.0。
- 不修改现有数据库结构和已有卡密数据。
- appstore / appstore_v2 响应外层字段保持不变。
- GitHub 在线更新继续强制 HTTPS + SHA256。
