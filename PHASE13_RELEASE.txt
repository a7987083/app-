ZONOE 软件源 Release 2026091908
版本：2026091908
基线：2026091907
阶段：IPA OpenList Saved-Config Hotfix
发布分支：release/2026091905-phase20-setting-hotfix
发布状态：准备触发正式 Release Workflow

更新内容：
1. IPA 设置页恢复成熟 OpenList 行为：先保存配置，再测试已经持久化并成功解密的配置。
2. IpaSourceConfig 增加 testSaved()，兼容 testInput() 统一转入已保存配置测试。
3. 保存后执行 runtime 配置读回与 Token 加密/解密 round-trip 校验；Token 留空保留旧值，首次配置仍必须填写 Token。
4. 页面安全展示 Token 解密失败原因，不回显 Token 明文。
5. 测试连接不再提交当前未保存表单，避免浏览器自动填充状态与服务器持久化状态混淆。
6. OpenList /api/fs/list、Authorization 原始 Token、/a/app 内部路径等协议保持不变。
7. 不修改 appstore/appstore_v2 协议、授权、缓存隔离、Phase 20 schema 与原 ZONOE Source Release workflow 语义。

在线更新：
- 目标 GitHub Release 标签：source-v2026091908。
- 资产：zonoe-online-update.zip + zonoe-online-update.zip.sha256。
- VERSION、public/update/ver.txt、ver.json 同步升级到 2026091908。
- file_sign 暂保持 8be29c04c34ba1d1cc7ec77d392b2bee；正式 Release workflow 会重新计算并严格校验。
- 发布完成后需从 2026091907 -> 2026091908 执行真实在线更新 E2E。
