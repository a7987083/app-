ZONOE 软件源 Release 2026092410
版本：2026092410
基线：source-v2026092409
阶段：Dylib CRUD 与 Legacy API Codegen 完善
发布分支：release/2026092410-dylib-crud-legacy-codegen
发布状态：正式发布候选（由 ZONOE Source Release CI 完成发布）

更新内容：
1. Dylib 列表取消“已有业务历史禁止删除”的限制；删除时事务清理版本、旧游戏授权、对应验证日志，再删除 Dylib 本体。
2. 版本管理新增编辑与删除入口；编辑复用现有 saveVersion(id) 更新能力，新增 deleteVersion() 物理删除指定版本。
3. OC Codegen 升级到 Generator 2.1.0；继续自动读取 Dylib、版本、Bootstrap、API Endpoint 与验证密钥。
4. 恢复并显式输出旧接口兼容信息：Index::dylib() -> /index/index/dylib，Index::apiface() -> /index/index/apiface。
5. 生成的 Config 与 GeneratedConfig/INTEGRATION 文档增加 legacyDylibURLs / legacyApiFaceURLs，方便旧工程继续接入。
6. 不修改现有 DylibVerificationService、Runtime Config、v1/v2 HMAC canonical、权限模型和数据库结构。
7. 2409 的 5 Tab Dylib Center UX 保持不变。
8. 在线更新继续沿用现有 UpdateManager / UpdateInstaller；2410 无新增 SQL migration。

验证：
- OC Codegen CI #9 / Run 36230506336：success。
- PHP 7.0 lint：success。
- JavaScript syntax：success。
- OC Codegen contract：success，覆盖 legacy Index API URL 输出。
- Dylib Center UX contract：success。
- MySQL 5.7 migration regression：success；无新增 migration。

在线更新：
- 目标升级：source-v2026092409 -> source-v2026092410。
- 正式 Release 必须包含 zonoe-online-update.zip 与 zonoe-online-update.zip.sha256。
- 继续执行 GitHub Release 检测、SHA256、备份、文件覆盖、版本写入、完整性校验与失败回滚。
