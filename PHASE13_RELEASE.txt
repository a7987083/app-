ZONOE 软件源 Release 2026091710
版本：2026091710
基线：2026091709

更新内容：
1. 重新定位 /appstore 回归起点为 2026091704，而不是 2026091707。
2. 2026091703 与 2026091704 的 application/route.php 完全一致，appstore 仍映射 index/App/list，路由本身未丢失。
3. 2026091704 首次把 fa_category.renewal_entry 加入公开软件源查询字段；生产库缺该列时会直接 SQL 异常。
4. SourceAppRecord::publicSourceColumns() 现在运行时检测 renewal_entry 是否真实存在；缺失时自动排除，源接口继续使用旧字段返回。
5. renewal_entry 存在时继续启用续费入口语义，不移除 renewal_entry 功能。
6. 数据库迁移失败、漏跑或历史库结构不完整，不再允许单个可选字段拖死整个 /appstore。
7. 不改变 appstore / appstore_v2 加密协议、客户端 JSON 字段和既有卡密授权协议。
8. 新增 PHP 7.0 缺列兼容回归；MySQL 5.7 测试覆盖迁移前旧表可查询、迁移后自动恢复 renewal_entry 查询。

影响版本：
- 2026091703：最后一个无 renewal_entry 强依赖的版本。
- 2026091704：回归起点。
- 2026091705~2026091709：继承相同查询依赖，生产库字段缺失时仍可能失败。
- 2026091710：首次从运行时代码层彻底消除该强依赖。

数据库：
- 不删除任何字段或业务数据。
- 仍保留既有 renewal_entry 迁移；迁移成功时功能完整启用。
- 迁移未成功时 /appstore 自动按旧表兼容模式运行。

在线更新：
- GitHub Release 标签：source-v2026091710。
- 资产：zonoe-online-update.zip + zonoe-online-update.zip.sha256。
- 2026091704~2026091709 可直接在线升级到 2026091710。
- 发布必须通过 PHP 7.0、MySQL 5.7、ZIP/SHA256 和真实 GitHub Release 在线升级 E2E 后才视为完成。

兼容性：
- 兼容宝塔 PHP 7.0、MySQL 5.7。
- 不要求客户端更新。
