ZONOE 软件源 Release 2026091703
版本：2026091703
基线：2026091702

更新内容：
1. 三种卡密统一恢复为一次性首次激活凭证；任意已激活卡密再次提交均返回“解锁码已使用”。
2. 后续持续验证统一走 Index::apiface，不再通过重复提交仅验证卡获取新签名。
3. apiface 保留 code/msg/expire/ts/nonce/sign 原字段和旧 HMAC 原文 udid|expire|ts|nonce。
4. sign 后新增 authorizations，仅列当前已激活且未过期的授权类型；不存在的类型不返回。
5. authorizations 按 card_scope=1/2/3 分别返回全软件源/仅验证/指定App的独立 expire。
6. 授权总览“换绑记录 / 授权事件 / 系统诊断”改为本页下方预览，不再打开独立页面。
7. 数据完整性审计扩展卡状态、scope、kmyp、时间链、指定App映射、日志孤儿记录及黑名单一致性检查。
8. 修复数据完整性“重新审计”路由错误导致 404，现指向 integrity/index。
9. 在线更新清单补齐 Authorization.php、authorization/index.html 等本版本文件。
10. 新增 Phase17.2 PHP 7.0 专项 CI，并同步 Phase15 契约；相关 CI 全部通过。

数据库：
- 本版本不新增数据库结构。
- 继续使用既有 card_scope、fa_kami_app、fa_authorization_event、fa_card_transfer_log 和 unlock_sign_key。
- 既有迁移保持幂等，可重复执行。

在线更新：
- GitHub Release 标签：source-v2026091703。
- 资产：zonoe-online-update.zip + zonoe-online-update.zip.sha256。
- 从 2026091702 可通过后台 GitHub 在线更新直接升级到 2026091703。
- 保留 HTTPS、SHA256、数据库/程序备份、文件覆盖校验、失败回滚和真实 Release E2E。

兼容性：
- 兼容宝塔 PHP 7.0、MySQL 5.7。
- 不改变卡密格式、时长定义、旧 sign 字段顺序和 HMAC 原文。
- 新增 authorizations 固定追加在 sign 后面。
